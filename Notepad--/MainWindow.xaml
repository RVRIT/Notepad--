<Window x:Class="Notepad.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Notepad" Height="450" Width="800">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>
    <DockPanel LastChildFill="True">
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="File">
                <MenuItem Header="New File"     Command="{Binding NewFileCommand}"/>
                <MenuItem Header="Open File..."    Command="{Binding OpenFileCommand}"/>
                <MenuItem Header="Save File"    Command="{Binding FileActiv.SaveFileCommand}"/>
                <MenuItem Header="Save File As..." Command="{Binding FileActiv.SaveAsFileCommand}"/>
                <MenuItem Header="Close All Files"/>
                <MenuItem Header="Exit"/>
            </MenuItem>
            <MenuItem Header="Search">
                <MenuItem Header="Find..."/>
                <MenuItem Header="Replace..."/>
                <MenuItem Header="Replace All..."/>
                <MenuItem Header="All tabs" IsCheckable="True"/>
            </MenuItem>
            <MenuItem Header="View">
                <MenuItem Header="Toggle Tree View"  Command="{Binding ToggleTreeViewCommand}"/>
            </MenuItem>
            <MenuItem Header="Help">
                <MenuItem Header="About" ToolTip="Bulgaru Bogdan Florin 10LF241"/>
            </MenuItem>
        </Menu>
        <TreeView DockPanel.Dock="Left"
          Width="200"
          ItemsSource="{Binding ExplorerRoots}"
          Visibility="{Binding ShowTreeView, Converter={StaticResource BoolToVis}}">
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem">
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                </Style>
            </TreeView.ItemContainerStyle>
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <TextBlock Text="{Binding Name}"/>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
        <TabControl ItemsSource="{Binding FileDeschise}"
                    SelectedItem="{Binding FileActiv}">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Name}"
                                   VerticalAlignment="Center"/>
                        <TextBlock Text=" *"
                                   VerticalAlignment="Center"
                                   Visibility="{Binding IsEdited, Converter={StaticResource BoolToVis}}"/>
                        <Button Content="✕"
                                Margin="6,0,0,0"
                                Width="16" Height="16"
                                FontSize="9"
                                Padding="0"
                                VerticalContentAlignment="Center"
                                HorizontalContentAlignment="Center"
                                Command="{Binding DataContext.CloseFileCommand,
                                          RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}"/>
                    </StackPanel>
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <TextBox Text="{Binding Content, UpdateSourceTrigger=PropertyChanged}"
                             AcceptsReturn="True"
                             AcceptsTab="True"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             FontFamily="Consolas"
                             FontSize="14"/>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>
    </DockPanel>
</Window>   